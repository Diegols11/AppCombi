@model IEnumerable<AppCombi.Models.Pasajero>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombrePasajero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Edad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DniPasajero)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombrePasajero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Edad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DniPasajero)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PasajeroID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PasajeroID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PasajeroID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
